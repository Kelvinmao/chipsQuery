CREATE TABLE CHIPS(
	MODEL_ID	       TEXT	   NOT_NULL	   DEFAULT '0',
	CHIP_NAME	       TEXT	   NOT_NULL	   DEFAULT 'Unknown',
	FUNCTIONS	       TEXT	   NOT_NULL 	   DEFAULT 'Unknown',
	PIN_NUMBER	       INT	   NOT_NULL	   DEFAULT 0,
	PIN_DEFINATION         TEXT        NOT_NULL        DEFAULT 'Unknown',
	CHIP_INTRODUCTION      TEXT        NOT_NULL	   DEFAULT 'Unknown'
);

CREATE TABLE `chipsdatabase`.`chips` (
  `MODEL_ID` TEXT NOT NULL DEFAULT 'unknown',
  `CHIP_NAME` TEXT NOT NULL DEFAULT 'unknown',
  `FUNCTIONS` TEXT NOT NULL DEFAULT 'unknown',
  `PIN_NUMBER` INT NOT NULL DEFAULT 0,
  `PIN_DEFINATION` TEXT NOT NULL DEFAULT 'unknown',
  `CHIP_INTRODUCTION` TEXT NOT NULL DEFAULT 'unknown',
  `CHIP_ID` INT NOT NULL AUTO_INCREMENT,
  PRIMARY KEY (`CHIP_ID`));
